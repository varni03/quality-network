[project]
name = "quality-network"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.115.0",
  "sqlalchemy==2.0.35",
  "pydantic==2.9.2",
  "python-dotenv==1.0.1",
  "httpx==0.27.2",
]

[tool.vercel]
entrypoint = "backend/main.py"
